# write to secret/environment/directory/users/bind, content is the password for the
# AD user that is used by nexus and jenkins to look up users and groups

# write to secret/environment/directory/users/build/agent, content is the password for the
# AD user that is used by jenkins to connect agents to the master
